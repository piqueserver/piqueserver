language: python
dist: trusty
sudo: required

python:
  - 2.7

install:
  - pip install -r requirements.txt
  - pip install -r dev-requirements.txt

script:
  - python setup.py install
  - pylint piqueserver
  # - pylint pyspades
